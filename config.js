/* ============================================================================
   إعدادات الموقع — تُعدّل مرة واحدة فقط · Site settings — edit once
   ----------------------------------------------------------------------------
   سجلّ المعلّم المركزي (اختياري): ضع هنا رابط تطبيق Google Apps Script
   لتجميع نتائج كل طالب في جدول Google خاص بك. اتركه فارغاً لإيقاف الإرسال.

   Central teacher log (optional): paste your Google Apps Script Web-App URL
   between the quotes to collect every student's result into your private
   Google Sheet. Leave it empty ("") to disable sending.
   Setup steps are in MASTER-LOG-SETUP.md.
   ============================================================================ */
var LOG_ENDPOINT = "";
